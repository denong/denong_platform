<%= form_for(@customer_reg_info) do |f| %>
  <% if @customer_reg_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_reg_info.errors.count, "error") %> prohibited this customer_reg_info from being saved:</h2>

      <ul>
      <% @customer_reg_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
