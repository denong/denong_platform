<%= form_for(@yl_trade) do |f| %>
  <% if @yl_trade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yl_trade.errors.count, "error") %> prohibited this yl_trade from being saved:</h2>

      <ul>
      <% @yl_trade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
