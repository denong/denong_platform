require 'rails_helper'

RSpec.describe "identity_verifies/edit", type: :view do
  before(:each) do
    @identity_verify = assign(:identity_verify, IdentityVerify.create!())
  end

  it "renders the edit identity_verify form" do

  end
end
