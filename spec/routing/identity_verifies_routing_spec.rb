require "rails_helper"

RSpec.describe IdentityVerifiesController, type: :routing do
  describe "routing" do

    # it "routes to #index" do
    #   expect(:get => "/identity_verifies").to route_to("identity_verifies#index")
    # end

    # it "routes to #new" do
    #   expect(:get => "/identity_verifies/new").to route_to("identity_verifies#new")
    # end

    # it "routes to #show" do
    #   expect(:get => "/identity_verifies/1").to route_to("identity_verifies#show", :id => "1")
    # end

    # it "routes to #edit" do
    #   expect(:get => "/identity_verifies/1/edit").to route_to("identity_verifies#edit", :id => "1")
    # end

    # it "routes to #create" do
    #   expect(:post => "/identity_verifies").to route_to("identity_verifies#create")
    # end

    # it "routes to #update" do
    #   expect(:put => "/identity_verifies/1").to route_to("identity_verifies#update", :id => "1")
    # end

    # it "routes to #destroy" do
    #   expect(:delete => "/identity_verifies/1").to route_to("identity_verifies#destroy", :id => "1")
    # end

  end
end
